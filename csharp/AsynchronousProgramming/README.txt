* When running in Debug configuration, or when running in the Debugger, performance timings will
  not be accurate. They can often show the parallel version taking longer than the single-threaded
  version. For accurate timings, compile in Release mode and run without the debugger (CTRL-F5).